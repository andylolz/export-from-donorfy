# Export from Donorfy

## Setup

Clone this repo, then:

```
pip install -r requirements.txt
cp .env.example .env
```

…Then add the necessary env variables to `.env`.

## Usage

```
source .env
python app.py
```
